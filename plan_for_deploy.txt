================================================================================
PLAN FOR DEPLOY — NBA Player Performance Forecaster
Status: EXECUTED (both agent workstreams shipped 2026-06-04)
Last updated: 2026-06-06
================================================================================

This file was the deployment plan that split the "make NBA production-ready"
push into two parallel Claude-agent prompts (Agent A = backend/data/model/
scraping, Agent B = frontend parity + deploy). Both agents completed their
workstreams; the original paste-ready prompts have been removed now that the
work is done. What remains below is (1) a record of what shipped and (2) the
genuinely-open follow-ups that were explicitly deferred.

Live tracker with full detail: notes.txt. Deploy/runbook docs: docs/DEPLOYMENT.md.

--------------------------------------------------------------------------------
WHAT SHIPPED
--------------------------------------------------------------------------------

Backend (Agent A):
  - All distributions work end-to-end. `negative_binomial` added to
    `SUPPORTED_DISTRIBUTIONS` (+ aliases) so the model vocabulary and the
    chart-overlay vocabulary match via `normalize_distribution_name`. A
    parametrized test proves every supported family round-trips through
    `run_single_prop`.
  - `DEFAULT_DISTRIBUTION_BY_STAT` set from the 2026-06-04 sweep
    (rebounds=poisson; points/assists/pra=normal, sweep inconclusive — see
    the methodology caveat in README "Current defaults").
  - Hourly self-update: `nba_model/data/hourly_update.py` (Chrome :9222 +
    Playwright preflight, fcntl lockfile, per-run JSON report) +
    `scripts/scheduler/hourly_update.sh` + `com.nba.hourly.plist` (launchd,
    StartInterval 3600) + cron fallback. Playwright guard + CDP auto-reconnect
    added to `daily_etl.py`.
  - `data/config/web_text_urls.txt` refreshed: dropped dead PrizePicks dup,
    underdogfantasy.com -> underdogsports.com, all 9 active books covered.
  - ETL hardening: `players` table backfilled from `game_logs.matchup`
    (528/531 teams set); hourly run recomputes predictions so the deployed
    model is genuinely updated every hour.
  - `nba_model/model/manual_lines.py` shared parser (+ `persist_*` writer);
    desktop UI delegates to it. Stable public API for the web view.
  - `nba_model/web/subscriptions.py` is now an env-selected backend
    dispatcher (SQLite default, Postgres via `SUBSCRIPTIONS_DB_URL`); webhook
    + auth callers unchanged. DEPLOYMENT.md §13–14 document Postgres setup +
    the hourly-host -> cloud DB delivery approach.

Frontend / deploy (Agent B):
  - New web views: Single prop (full tuning), Manual lines import
    (admin-only DB save), Operations console (admin-only, always — even in
    open-access mode). Distribution selectors read
    `simulation.SUPPORTED_DISTRIBUTIONS` (no hardcoded lists).
  - Line-display upgrade: no static matplotlib left in NBA web views; the
    distribution figure highlights best-EV over/under (gold-bordered star),
    declutters per-book markers onto a top rail, and offers a compact
    "line ladder" toggle. Bonus `build_line_movement_figure` ready to wire.
  - Deploy verified: open-access (BILLING_ENABLED unset/0) boots clean on
    SQLite; `BILLING_ENABLED=1` re-engages OIDC + Stripe; Docker serves all
    views; webhook still runs. DEPLOYMENT.md §9–12 + README updated.

Tests: 239 -> 312 passing.

--------------------------------------------------------------------------------
REMAINING OPEN ITEMS (deferred; not launch-blocking for open-access NBA)
--------------------------------------------------------------------------------

  - Stub scrapers: 11 of 20 books still parser-less. Each needs an
    authenticated NBA-board snapshot, then a parser following the
    pick6.py (DFS) / betmgm.py (sportsbook) template.
  - DB delivery automation: the actual `git add/commit/push` (or object-
    storage) post-hook that publishes the hourly-host DB to the cloud app is
    documented in DEPLOYMENT.md §14 but not yet implemented.
  - Distribution-default re-rank: re-run the sweep with `--use-market-lines`
    once `betting_lines` market coverage is backfilled beyond the
    2026-03-14..15 window (the current `line = mean` default makes symmetric
    families degenerate and dominates the ranking by construction).
  - Multi-sport: `sports/` scaffolding is in place (NBA live; NFL/MLB/NHL/
    soccer stubs). Real per-sport ingestion + scraping is the next big
    workstream — see docs/MULTI_SPORT_PLAN.md.
  - Nice-to-haves: Stripe Customer Portal link (self-serve cancel),
    most-recent line per book per UPCOMING game, historical odds/snapshot
    backfill, auto-detect session expiry within the ETL run.

================================================================================
END OF PLAN
================================================================================
