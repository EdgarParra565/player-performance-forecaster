# NBA Player Performance Forecaster - Working Notes

Last updated: 2026-07-22

Active tracker only: architecture, what's done, what's next. CLI runbooks,
deployment, and security live in **README.md** and **docs/** — not duplicated here.

Test baseline: **702 passing** (`.venv/bin/python3 -m pytest nba_model/tests -q`;
Windows: `.venv\Scripts\python.exe -m pytest nba_model/tests -q`).

---

## Near-term objectives (NBA)

1. **In-season data loop** — Mac Chrome `:9222` → hourly ETL → `publish_db.sh` → cloud app.
   This is the gating item: WS10 validation and TRUE-arb detection are data-starved without it.
2. **Run WS10 validation** — market-line backtests, 4–8 week paper trade
   (`bet_slip` → `bet_log` → `calibration_report`), CLV review. Tooling is built;
   only live data is missing. **Do not bet real money until this passes.**
3. **More scrapers** — 10 of 20 books produce parsed rows (VegasInsider now feeds real
   11-book odds into `betting_lines`). theScore/DraftKings/Fliff fetch paths are fixed but
   NBA-offseason-empty — their extractors are the first preseason task. Remaining blocks
   are external (mobile-only / paywalled / 21+), not code. See scraper follow-ups below.
4. **Multi-sport** — MLB data layer is beta (off NBA consensus path). NFL scaffold parked.
   NBA deploy does not require shipping MLB/NFL web views.

---

## Architecture (compact)

- **Stack:** Python 3.11, SQLite `data/database/nba_data.db`, Streamlit + Tk, pytest.
- **Ingest:** `nba_results_ingestion.py`, `daily_etl.py`, `hourly_update.py` (Mac host).
- **Scrapers:** `nba_model/scrapers/` (20 registered); fetch via CDP + real Chrome `:9222`.
- **Model:** `simulation.py`, `prop_board.py` (shared projection helpers),
  `edge_scanner.py`, `cross_book_arb.py`, `blend_team_prior()`.
- **Validation:** `evaluation/bet_slip.py`, `calibration_report.py`, `clv_proxy.py`,
  `bet_log` table.
- **Web:** `nba_model/web/app.py` (+ `cross_book_view.py` helpers) + auth/billing
  (`BILLING_ENABLED=0` = open access).

**Constraints:** `.venv/bin/python3` for ETL/browser; admin-gates on Operations + manual-lines
save; web = Plotly, Tk = matplotlib; do not delete `sports/` scaffolding.

---

## Implemented (trust unless regression)

- **Edge Scanner** — `edge_scanner.py` + "Line edge scanner" Streamlit view; CLI
  `--books underdog ...`. Three modes (`MODEL_MODES`): `chart_mean` (default quick
  screen: last-N normal), `rolling`, and **`full`**. Output keeps `SCORED_COLUMNS`
  as ordered prefix + appends `distribution`/`model_mode` (`SCORED_COLUMNS_FULL`).
  DFS books assume -110. Unfittable rows are dropped, never raised.
- **WS9 unify scoring (2026-07-16)** — `full` mode runs the prop_board stack
  (rolling μ/σ + `blend_team_prior` + `get_default_distribution(stat)`), DB-direct
  (no DataLoader/network). Scanner full mode and hourly `predictions` share
  `prop_board.build_history_from_games` / `project_stat_moments` /
  `project_prop_line`, so they can't drift (parity pinned by test).
- **WS8 cross-book layer (2026-07-16)** — `cross_book_arb.py`:
  `find_cross_book_opportunities` (per (player, stat) with ≥2 books: line_gap,
  best over/under book, middle candidates), `fetch_two_way_lines` +
  `detect_two_way_arb` (TRUE-arb flag only when RAW
  `implied_over_A + implied_under_B < 1` in an executable direction, from REAL
  `betting_lines` odds — never from assumed -110). CLI `--min-gap` / `--arb-only`.
  Streamlit **"Cross-book"** view (KPIs incl. arb count, distinct true-arb section,
  CSV export, Player Charts jump, offseason empty-state); its book list unions
  `web_prop_cards` + `betting_lines` so real-odds-only books stay selectable.
- **WS10 tooling (2026-07-16; measurement only, no execution layer)** —
  `bet_log` table + `insert_bet_log_rows` / `settle_bet_log(fill_clv=)` (grades
  pending picks vs `game_logs`, fills `clv_delta` from closing
  `betting_line_snapshots`); `bet_slip.py` CLI = the paper-trade entry point
  (full-mode edges → min_edge/min_p/max_picks/capped-Kelly gates → CSV+JSON
  artifacts + `bet_log` insert; `--dry-run` = no side effects);
  `calibration_report.py` (per-stat reliability buckets + Brier from settled
  `predictions` or `bet_log`); `hourly_update --settle-bet-log` (default OFF).
- **Full model path** — 8 distributions; team priors in `run_single_prop`, `prop_board`,
  hourly predictions (points/assists/rebounds/pra). Rebounds default → poisson; others → normal.
- **Player Charts** — Plotly web + matplotlib Tk; line ladder, movement tab, CLV/Kelly,
  splits, ribbon, etc. Upcoming-game line selection + scrape staleness timestamps.
- **Web / billing** — Feature parity with Tk; watchlist (cookie + signed-in JSON); Stripe
  portal, admin dashboard, trial via `created_at`, Postgres subs via `SUBSCRIPTIONS_DB_URL`.
- **Ops** — `publish_db.sh`, ETL alerts, hourly scheduler, login-wall detection,
  `historical_odds_backfill.py`, FanDuel/BetRivers/ESPNBet team-line parsers (fixtures).
- **Multi-sport** — `sport` column migration; `(book, sport)` scraper resolution; MLB Stats
  API ingest + `mlb_game_logs`; FanDuel MLB team lines validated live; NFL scaffold only.
- **Scraper mission (2026-07-21)** — **VegasInsider aggregator parser**
  (`scrapers/vegasinsider.py` + `data/vegasinsider_odds_ingestion.py`): real American
  odds from ~11 books → `betting_lines` with `source='vegasinsider'` provenance
  (additive nullable `source` column, idempotent migration); 462 rows landed from the
  stored May capture. **Fetch-path infra:** CLI now actually forwards
  `--chrome-debug-port` (was silently dropped → plain-requests path); generic
  per-book `BookScraper.scroll_page` flag (DK lazy grid); iframe-aware
  `js_extractor` pattern (Fliff emulator iframe). **espnbet → theScore Bet** rebrand
  (`sportsbook.thescore.bet`, old ENV/cert blocker cleared). Working URLs added to
  `web_text_urls.txt`. Blocked-with-proof: Fanatics/Sleeper/Dabble (app-only),
  BettingPros (paywalled), HardRock (21+), OddsShark (projections, not odds).

---

## Open work (priority)

### WS10 — Validation before auto-bet (tooling built; the runs are what's left)

- [ ] **Phase 0** — populate `web_prop_cards` / `betting_lines` in-season (Mac ETL),
      `historical_odds_backfill`, nightly `publish_db.sh` cadence.
- [ ] **Phase 1** — `--use-market-lines` sweeps/backtests, CLV vs close, calibration
      review, promotion gates. Tools exist; runs blocked on Phase 0 data.
- [ ] **Phase 2** — paper-trade 4–8 weeks: `bet_slip` (drop `--dry-run`) → settle →
      `calibration_report --source bet_log`; weekly review.
- [ ] **Phase 3** — execution layer ONLY if Phases 1–2 pass (book APIs, limits, kill switch).

### Smaller follow-ups

- [x] Expose `full` mode in the **Line edge scanner** view's model toggle
      (2026-07-21) — "Full model (beta)" added to the selector, mirroring the
      Cross-book view: shared label/fallback contract via
      `nba_model/web/edge_scanner_view.py` (re-exports `resolve_model_mode`),
      graceful degrade to chart_mean when unavailable. Preview limits + scan
      throttle apply to `full` unchanged. Tests: `test_edge_scanner_view.py`.
- [x] Root-fix NaN moments (2026-07-21): `build_history_from_games` now recomputes
      NaN rolling μ/σ over the surviving games when a NULL stat value lands inside the
      window (available-case; `_repair_nan_stat_moments`, floor
      `MIN_VALID_WINDOW_FRACTION`=0.5 → ≥5 of 10, min 2). Below the floor μ/σ stays NaN
      and the row is skipped. `_persist_predictions` also drops non-finite rows (defense
      in depth) and `db.delete_nonfinite_predictions()` cleans any pre-fix NaN rows
      (SQLite stores NaN REAL as NULL). Parity with Edge Scanner full mode preserved;
      clean-window μ/σ unchanged.
- [x] Team charts beyond lobby `points` (2026-07-21): per-game team aggregates already
      cover points/assists/rebounds/pra/3pm/fgm (`_team_value_sql_expr`; minutes stays
      out — team sum is ~constant and the SQL allowlist forbids it). New for non-points:
      a **props-derived team reference** = Σ of the team's players' cross-book consensus
      prop lines (`_fetch_props_derived_team_reference`), rendered as a distinct dotted
      line labeled "props-derived team mean", never a book line. Coverage floor
      `DERIVED_TEAM_REF_MIN_PLAYERS`=5 (empty-state below it; offseason usually empty);
      roster via `game_logs.matchup` (players.team sparse); staleness via `since_hours`
      (validated by `iv.validate_min_players` / `validate_since_hours`). Points path
      byte-identical. Tests: `test_team_props_reference.py`.
- [x] Structured logging (2026-07-21): shared `nba_model/logging_utils.py` (stdlib
      only; JSON-lines file formatter with UTC ISO ts + `extra=` fields, human
      console, `LOG_LEVEL` env, `get_logger` / `configure_logging`). Migrated
      daily_etl, hourly_update, publish_db, nba_results_ingestion, bet_slip,
      calibration_report — report/alert/exit-code contracts unchanged; per-step
      `step` / `status` / `duration_ms` fields added. `webhook_app` left untouched
      (strict A09 redaction; its tests stay green). TODO: migrate scraper/fetch path
      to logging_utils once scraper work lands. Note: `market_reverse_engineering`
      calls `logging.basicConfig` at import, which no-ops daily_etl's own file
      handler (pre-existing; hourly uses `force=` and is unaffected). Tests:
      `test_logging_utils.py`.
- [ ] MLB per-game player-props parser + park factors.

### Scraper follow-ups (post-2026-07-21 mission: shortcomings + next steps)

Known shortcomings of what shipped:

- **VegasInsider is over-only** — the grid posts over cells (`o8.5 -105`); rows land
  with `under_odds` NULL. Great for `--use-market-lines` / CLV / line shopping, but
  `detect_two_way_arb` needs real odds on BOTH sides, so TRUE-arb stays data-starved
  until a two-sided odds source lands (sportsbook extractors below, or check whether
  VI exposes under odds on per-player/expanded pages).
- **Single-aggregator drift risk** — the VI parser is pinned to the May-2026 grid
  layout (fixture-tested). Aggregator markup drifts; re-validate on the first
  in-season capture before trusting hourly ingestion.
- **462 rows = one May snapshot** — a proof-of-pipe, not volume. Value arrives only
  with an in-season fetch cadence.
- **theScore / DraftKings / Fliff are fetch-fixed but extractor-less for NBA** —
  offseason boards were legitimately empty, so their parsers could not be authored
  against real NBA content. Fetch proofs are stored; parsers are deferred, not done.

Next steps (in order):

- [ ] **Wire VegasInsider into the ETL cadence** — `vegasinsider_odds_ingestion` is a
      standalone CLI today; hourly ETL only stores the snapshot. Add the ingestion
      step to `hourly_update` / `daily_etl` (after the structured-logging work lands
      to avoid conflicts) so fresh grids auto-flow into `betting_lines`.
- [ ] **Preseason capture sprint (~Oct)** — re-fetch and author the deferred
      extractors against real NBA content: theScore `team_line_extractor`,
      DraftKings NBA validation (scroll path), Fliff `prop_preprocess` (PP-style
      More/Less), VI grid re-validation; re-check the DFS boards
      (PrizePicks/Underdog/Pick6/ParlayPlay) per the SEASON blocker row.
- [ ] **BetRivers re-login** (`:9222` Chrome) — still the one AUTH blocker; parser
      already exists, needs a live-capture validation.
- [ ] **App-only books (Fanatics/Sleeper/Dabble) + HardRock (21+)** — unscrapeable on
      web. If their odds matter, the path is The Odds API bookmaker keys (see README
      "smaller or alternate books"), not scraping. Revisit HardRock later.

---

## Scraper / season blockers (2026-07-21 snapshot)

Re-run fetch→parse when blocker clears. NBA **offseason** = empty lobbies, not parser bugs.

| Blocker | Books / action |
|---------|----------------|
| **AUTH** | BetRivers NBA — re-login in `:9222` Chrome; validate parser on live capture. |
| ~~**ENV** ESPNBet~~ **CLEARED (2026-07-21)** | Rebranded **theScore Bet** (`sportsbook.thescore.bet`; `espnbet.com` kept as alias, book name stays `espnbet`). Loads cleanly over CDP — no cert/ENV error. NBA offseason renders FUTURES only (no game lines); team_line_extractor awaits in-season game capture. |
| ~~**RENDER** DraftKings~~ **MLB LIVE-VERIFIED (2026-07-22)** | DK migrated its lobby grid to `event-cell` nodes — stale `sportsbook-outcome` selectors matched nothing so the fetcher skipped its content wait (nav-shell-only captures). Fixed: `event-cell` wait selector + `body.innerText` `js_extractor` (generic HTML-parse drops DK's virtualized grid). DK **MLB team lines now land 84 rows / 11 games** into `web_team_lines` (sport='mlb') via a new DK-order block regex (`extract_team_lines_dk`). DK NBA board is offseason-empty (futures only); its NBA extractor still awaits an in-season capture but its fetch is now robust (24.9KB fresh capture). |
| **IFRAME** Fliff | Fetch path iframe-aware (`scrapers/fliff.py:extract_iframe_text`); domain `getfliff.com` (+`fliff.com` alias). Re-verified 2026-07-22: live capture = 3.3KB real board with a full **MLB/WNBA/MLS slate** — but the `channelId=-333` board shows **game moneylines**, not PP-style More/Less **player** props, so `prop_preprocess` still cannot be authored against real content. MECH-VERIFIED fetch only; parser deferred until a More/Less player board renders. |
| **MOBILE-ONLY** Fanatics | betfanatics.com/sportsbook is marketing-only on the web (Download-App / promo banners, 0 line tokens) — real board is app-gated. Live-probed 2026-07-21. Blocked-with-proof; stub kept. |
| **GATED/THIN** BettingPros | `/nba/odds/` + `/nba/odds/player-props/` render nav + a Premium/"Upgrade" wall; the grid is JS/subscription-gated and server-side-thin (2.5KB, stale cached date). Blocked-with-proof; stub kept. |
| **PROJECTIONS** OddsShark | `/nba/computer-picks` is model predictions (computer picks / predicted scores), not real book line/odds — out of scope for `betting_lines`/`web_*`. Stub kept, documented. |
| **MOBILE-ONLY (target-7)** | Sleeper (picks are app-only, not on web), Dabble (app-only), HardRockBet (account requires 21+). Marked blocked; stub configs kept. Revisit HardRockBet later. |
| **Parser TODO** | MLB player props (per-game pages); MLB team-line live pairing edge case; park factors. |
| **DONE (2026-07-21)** | **VegasInsider** — cross-book odds grid parser (`scrapers/vegasinsider.py:extract_odds_rows`) + `data/vegasinsider_odds_ingestion.py` → **real 11-book American odds into `betting_lines`** (`source='vegasinsider'`, over-only). 462 rows landed from the stored 2026-05-08 snapshot. First real-odds feed for `--use-market-lines` / cross-book line shopping. |

10 of 20 scrapers still stub-only (VegasInsider parses real odds; DK MLB team
lines now LIVE; theScore/Fliff/DK-NBA fetch paths fixed but NBA-offseason-empty).

---

## Parser verification matrix (2026-07-22)

Full end-to-end sweep of every parser-bearing scraper: fresh CDP fetch today →
snapshot stored → parser → rows in the right table. NBA is offseason (empty
boards ⇒ MECH not a bug); MLB is in-season (live rows). Verdicts:
**LIVE** = fresh fetch parsed >0 rows into the live DB today;
**MECH** = fresh fetch OK + session classified correctly + parser proven on the
best stored snapshot/fixture (only live NBA content missing);
**BLOCKED** = external blocker, recorded with proof.

| Book (sport) | Verdict | Fresh fetch today (chars / session) | Parser proof | DB insert proof | Fixed this pass |
|---|---|---|---|---|---|
| fanduel (mlb) | **LIVE** | 9,899 / content (not walled) | 84 rows / 14 games from today's snapshot | +84 → `web_team_lines` sport='mlb' (168→252); re-parse +0 (dedupe) | — |
| draftkings (mlb) | **LIVE** | 12,881 / content (grid rendered) | 84 rows / 11 games from today's snapshot | +84 → `web_team_lines` sport='mlb' (0→84); re-parse +0 (dedupe) | **YES** — `event-cell` wait selector + `body.innerText` js_extractor (grid render) + DK-order block regex `extract_team_lines_dk` + adjacency-collapse team pairing |
| vegasinsider | **MECH** | 15,317 / real page, grid offseason-empty (0 parsed) | 495 parsed → 462 resolved from 2026-05-08 snapshot | 462 rows `betting_lines` source='vegasinsider', 11 books ×42, plausibility 0-rejected, re-ingest 0-inserted / 462-dup | — |
| prizepicks | **MECH** | 801 / sign-in page, session expired (2/4 markers) — classified correctly | 20 cards (best stored board snapshots) | live `web_prop_cards` has 93 pp rows | — |
| underdog | **MECH** | 2,632 / lobby, offseason-empty (2/3 markers) | 105 cards (best stored snapshots; 2 filter snaps login-walled + skipped) | live `web_prop_cards` has 351 ud rows | — |
| pick6 | **MECH** | 988 / login-wall (correctly detected) | 23 cards each from sid 74 & 91 | live `web_prop_cards` has 46 p6 rows | — |
| parlayplay | **MECH** | 5,959 / lobby, offseason-empty (3/4 markers) | 12 cards (stored snapshot) | live `web_prop_cards` has 12 pp rows | — |
| betmgm | **MECH** | 6,997 / board nav, 0 games (offseason) | 24 rows from sid 37 (ma.betmgm.com) | live `web_team_lines` has 180 betmgm rows | — |
| caesars | **MECH** | 11,286 / board nav, 0 games | 24 rows from sid 31 | live `web_team_lines` has 192 caesars rows | — |
| draftkings (nba) | **MECH** | 24,891 / board nav, 0 games (fetch now robust) | 24 rows from sid 39 | live `web_team_lines` has 168 dk rows | (benefits from DK fetch fix above) |
| bovada | **MECH** | 3,105 / casino redirect (geo), 0 auth markers — classified correctly | 24 rows from sid 84 | live `web_team_lines` has 24 bovada rows | — |
| kalshi | **MECH** | 5,228 / login-wall (detected) | 18 moneyline rows from sid 41 | live `web_team_lines` has 74 kalshi rows | — |
| fanduel (nba) | **MECH** | 5,913 / futures only (2027 champ odds), 0 games | 6 rows from committed fixture | fixture-only (TODO real-capture stands) | — |
| espnbet/theScore | **MECH** | 13,879 / loads clean, futures only, 0 games | 6 rows from committed fixture | fixture-only (TODO real-capture stands) | — |
| betrivers | **BLOCKED (AUTH)** | 4,346 / **login-wall correctly detected** (1/4 markers) | 6 rows from committed fixture | login-walled — not counted as fetch proof | — (classification verified) |
| fliff | **MECH (fetch only)** | 3,310 / real board, live MLB/WNBA/MLS slate (moneylines, no More/Less props) | n/a — no player-prop content to author `prop_preprocess` against | — | — |

Full hourly-style pass (all 16 `web_text_urls.txt` URLs): **16/16 fetched, 0
failed**; both parsers ran clean, 4 login-walled snapshots skipped, 0 new inserts
(offseason ⇒ no line movement, change-only dedupe working). Loop verified
unattended.

Follow-ups surfaced (unchanged scope, recorded only): VegasInsider publishes an
MLB player-props page (60KB, has MLB markets) but the NBA over-cell grid parser
reads 0 rows there — different layout, not wired. DK NBA / theScore / FanDuel-NBA
game-line extractors still await an in-season NBA capture. Fliff `prop_preprocess`
awaits a More/Less player board.

---

## Model accuracy & Edge Scanner (honest baseline)

**Edge Scanner ≠ arbitrage.** It ranks **model edge** (forecast vs line), not locked profit.

| Term | Meaning | Built? |
|------|---------|--------|
| Model edge | P(side) vs book implied | Yes (Edge Scanner, 3 modes) |
| Line shopping | Best over/under line across books | Yes (`cross_book_arb` + Cross-book view) |
| Middle | Over low line + under high line | Candidates flagged via `line_gap` — never "guaranteed" |
| True arb | Σ implied < 1 across legs (real odds both sides) | Detector built (`detect_two_way_arb`); **data-starved** until sportsbook odds land in `betting_lines` |

**Distributions:** engineering solid; **validated market edge still unproven.** Only
rebounds→poisson showed positive sweep ROI (+8.8%, n=482, **not significant**).
Points/assists/pra stay normal (sweep inconclusive / negative ROI when bets placed).
`--use-market-lines` needs more `betting_lines` volume. High win rates in old
real-data benchmarks used rolling mean as the line (model vs itself) — not book CLV.

**WS9 closed the modeling gap; the remaining gap is proof:**

| | `chart_mean` (quick screen) | `full` (WS9) | Still missing |
|--|--|--|--|
| μ / σ | Last-N mean / sample std | Rolling + team priors | Defense/minutes adjustments in scanner path |
| Dist | Always normal | `get_default_distribution(stat)` | Per-stat σ tuning |
| Proof | None | None yet | Settled `bet_log` + CLV + market-line backtests (WS10) |

---

## Known limitations

- Sparse scraped lines → Edge Scanner / Cross-book / Line Movement / CLV mostly empty
  until the Mac ETL runs in-season (offseason arb count 0 = expected).
- `betting_lines` thin → market-line validation still volume-starved (VegasInsider now
  feeds real 11-book odds, but over-only + one May snapshot so far); TRUE-arb needs a
  two-sided real-odds source (backfill + aggregator tooling exists).
- Team charts: per-game aggregates for points/assists/rebounds/pra/3pm/fgm. Only
  `points` has a posted book reference (implied team total); other stats get a
  props-derived reference (Σ players' consensus prop lines) shown only when ≥5
  players have current props — thin/offseason coverage shows an empty-state.
  `players.team` sparse, so team membership uses the `matchup` fallback.
- Scraping host = dev Mac; dev box may be Windows (`DataLoader` sqlite leak on temp DB
  teardown — scanner `full` mode is DB-direct and unaffected).
- Hourly `predictions` NaN rows for NULL-in-window stats: fixed 2026-07-21 (root-fix in
  `build_history_from_games` + `_persist_predictions` guard + `delete_nonfinite_predictions`
  cleanup). Below the valid-games floor a row is intentionally skipped, not projected.
- **Do not use Edge Scanner / bet_slip picks for real money** until WS10 Phases 1–2 pass.

---

## New UI (flagship) — trading-terminal frontend

A brand-new, standalone frontend that will become the flagship surface. It is
**parallel** to Streamlit + Tk (both untouched, both still work). Backend work is
**additive**: a new read-only `api/` package only — no changes to `nba_model/`.

**Stack:** FastAPI (read-only) in `api/` + React/TypeScript/Vite/Tailwind v4 in
`frontend/`, charts via Apache ECharts, data via TanStack Query through Vite's
`/api` proxy. Run: `.venv/bin/python3 -m uvicorn api.main:app --port 8000` and
(in `frontend/`) `npm run dev` → http://localhost:5173. See `frontend/README.md`.

### Phase 1 — shipped (2026-07-22)

- **`api/` read-only service** — wraps the SAME functions Streamlit uses
  (`player_charts` fetchers, `edge_scanner.score_prop_edges`, `DatabaseManager`
  consensus/KPI queries). No writes, no duplicated model logic. Inputs validated
  through `nba_model/web/input_validation.py` (stat/team/season/n_games/
  rolling/since_hours). Pydantic response models, `/api/health`, `Cache-Control`
  headers (short max-age on data, no-store on health), `NBA_DB_PATH` override.
  Endpoints: `/api/health`, `/api/meta`, `/api/slate/{kpis,recent-games,edges}`,
  `/api/players/search`, `/api/players/{id}`. Any aggregate the data layer
  didn't already expose (KPI counts, freshest-scrape ts, player search) is a
  thin read-only helper in `api/services.py`, not a change to `nba_model`.
- **Tests** — `api/tests/test_api.py`, 11 cases, httpx TestClient against a temp
  DB seeded like the existing harnesses (active-players ref + game logs +
  web_prop_cards + a games matchup). Kept OUT of `nba_model/tests` so the 697
  baseline is untouched; run with `.venv/bin/python3 -m pytest api/tests -q`.
- **`frontend/` design system** — tokens (near-black surfaces, electric-green
  +EV / red -EV, mono tabular numerals) + components: StatCard, DataTable
  (sticky header + click-sort), ProbabilityBar (break-even tick), OddsBadge,
  LineSparkline, EmptyState, Delta, shared ECharts styling.
- **3 views, verified rendering vs the live DB (screenshotted):**
  1. Slate Dashboard — KPI row + top model edges (full mode) + recent-games strip.
  2. Player Detail (flagship) — searchable picker, recent-N chart w/ rolling
     mean + book-mean overlay, distribution + fitted normal + book-line markers,
     per-book P(over)/edge/EV table, hit-rate bars.
  3. Edge Scanner — dense sortable/filterable table, chart_mean|rolling|full
     model modes matching the CLI.
- Every view has a deliberate offseason empty-state showing "last data / last
  scrape" instead of a blank pane (LeBron detail renders fully from historical
  logs; live-line surfaces are correctly empty until October).

### Phase 2 / 3 backlog

- [ ] **Cross-book view** — wire `cross_book_arb` (line shopping / middles /
      true-arb) into a dedicated view, mirroring the Streamlit Cross-book layer.
- [ ] **Parlay builder** — multi-leg SGP with correlation, on top of the
      existing parlay model helpers.
- [ ] **Line-movement animations** — animate `betting_line_snapshots` drift per
      book (the data + Streamlit "Line Movement" tab already exist).
- [ ] **Code-split ECharts** — bundle is ~1.4 MB; lazy-load charts / manualChunks.
- [ ] **Auth + deploy story** — Phase 1 is local-only, no auth. Phase 3: reuse
      the existing OIDC/Stripe tier model, add a deploy target (the API is
      stateless + read-only, so it fronts the same published DB the cloud app uses).
- [ ] **Frontend tests** — component/e2e once the surface stabilizes (not required
      in Phase 1).
