# NBA Player Performance Forecaster - Working Notes

Last updated: 2026-03-02

This file is the active project tracker. It keeps only:
- current architecture
- what is already implemented
- what still needs to be done next



Need to get historic data for model to become more accurate
(ODDS_API) about ~$15 per month
---

## 1) Current Project Structure

```text
player-performance-forecaster/
├── nba_model/
│   ├── data/
│   │   ├── data_loader.py
│   │   ├── team_defense_ingestion.py
│   │   ├── daily_etl.py
│   │   └── database/
│   │       ├── db_manager.py
│   │       └── schema.sql
│   ├── model/
│   │   ├── feature_engineering.py
│   │   ├── defense_adjustment.py
│   │   ├── minutes_projection.py
│   │   ├── probability.py
│   │   ├── simulation.py
│   │   ├── correlation_calibration.py
│   │   ├── parlay_simulation.py
│   │   ├── parlay_ev.py
│   │   ├── odds.py
│   │   ├── odds_ingestion.py
│   │   └── line_tracking.py
│   ├── evaluation/
│   │   ├── backtest.py
│   │   ├── significance.py
│   │   ├── run_baseline_benchmark.py
│   │   ├── run_batch_backtest.py
│   │   ├── run_real_data_benchmark.py
│   │   ├── run_distribution_sweep.py
│   │   ├── line_comparison.py
│   │   └── monthly_diagnostics.py
│   ├── tests/
│   │   ├── test_smoke.py
│   │   ├── test_daily_etl.py
│   │   ├── test_odds_ingestion.py
│   │   ├── test_evaluation_tools.py
│   │   ├── test_context_features.py
│   │   └── ... (other tests)
│   ├── visualization/
│   │   └── distribution_plot.py
│   ├── run_model.py
│   └── simple_ui.py
├── README.md
├── requirements.txt
└── notes.txt
```

---

## 2) Implemented (Important)

- [x] Unified core stat schema (`points`, `assists`, `rebounds`, `minutes`)
- [x] Backtester supports distribution selection and significance output
- [x] Spread-aware minutes logic wired into backtest rows
- [x] Defense + home/away + back-to-back adjustments integrated in backtest
- [x] Context features added (`rest_days`, `travel_flag`, `injury_proxy`, schedule density)
- [x] Team defense ingestion + validation report (`--validate-only`, `--fail-on-missing`)
- [x] Daily ETL runner for game logs + team defense + odds (`daily_etl.py`) + scheduled GitHub Actions workflow
- [x] Odds ingestion hardening: retries, schema validation, dedupe, insert reporting
- [x] Real-data benchmark runner with per-player/window confidence intervals
- [x] Distribution sweep runner with ROI/calibration/significance summary exports
- [x] Cross-book and model-vs-book comparison engine
- [x] Monthly diagnostics (drift, drawdown, calibration trend)
- [x] Expanded test coverage for ETL, ingestion, evaluation tools, context features
- [x] Prop board generator for all player props in a game date (CSV-style board with model probabilities/EV)
- [x] Historical sportsbook snapshots table (`betting_line_snapshots`) populated from The Odds API
- [x] CLV-style proxy from snapshots (`clv_proxy.py`)
- [x] Market reverse-engineering tools (sigma-scale inference from book odds; `market_reverse_engineering.py`)
- [x] Market data quality checks for lines and snapshots (`market_data_quality.py`)

---

## 3) Current Active Priorities (Do Next)

### A. Run and review real benchmarks (highest priority)
- [ ] Run `run_real_data_benchmark.py` on 10+ players with windows `5/7/10/15`
- [ ] Run `run_distribution_sweep.py` on `points/assists/rebounds/pra`
- [ ] Review artifacts and pick default distribution by stat type

### D. Expand comparison and diagnostics outputs
- [ ] Add monthly report rollups by stat type and by player segment

---

## 4) Important Backlog (After Current Priorities)

- [ ] Historical game-log backfill beyond current season window
- [ ] Better injury features (not just minutes-drop proxy)
- [ ] Travel burden improvements (distance/timezone proxies)
- [ ] Cross-book normalization pass for naming/market aliases
- [ ] Example notebook for reproducible analysis walkthrough
- [ ] Remove or consolidate duplicate/legacy data loader paths

---

## 5) Known Gaps / Constraints

- Historical open/close line snapshots are now stored via `betting_line_snapshots`, but quality checks and downstream consumers are still evolving.
- Current market analysis depends on whatever is present in `betting_lines`.
- Some output scripts can produce empty artifacts when DB windows have no eligible rows.
- ETL scheduler exists (GitHub Actions), but external alerting/monitoring on failed runs is still minimal.

---

## 6) Quick Runbook Commands

```bash
# Daily ETL
python3 -m nba_model.data.daily_etl --strict

# Real-data benchmark (player/window CI exports)
python3 -m nba_model.evaluation.run_real_data_benchmark \
  --windows 5 7 10 15 \
  --stat-types points \
  --distributions normal \
  --start-date 2024-11-01 \
  --end-date 2025-03-15

# Distribution sweep
python3 -m nba_model.evaluation.run_distribution_sweep \
  --windows 5 7 10 15 \
  --stat-types points assists rebounds pra \
  --start-date 2024-11-01 \
  --end-date 2025-03-15

# Line comparison
python3 -m nba_model.evaluation.line_comparison \
  --start-date 2024-11-01 \
  --end-date 2025-03-15 \
  --stat-types points assists rebounds pra \
  --edge-threshold 0.02

# Monthly diagnostics
python3 -m nba_model.evaluation.monthly_diagnostics \
  --start-date 2024-11-01 \
  --end-date 2025-03-15 \
  --stat-types points assists rebounds pra
```


================================================================================
TECHNICAL DEBT / KNOWN ISSUES
================================================================================

- Daily ETL runner exists, but scheduler + monitoring/alerting are not fully wired yet.
- Historical sportsbook snapshots (open/close timeline with timestamps) are now stored in `betting_line_snapshots`, but validation, CLV consumers, and sigma-scale reverse-engineering outputs are still experimental and require more data.
- Line comparison/monthly diagnostics can return empty artifacts when `betting_lines` or `predictions` are sparse.
- Real-data benchmark/distribution sweep scripts exist, but production defaults are not yet selected from a full run.
- Current travel/rest/injury features are heuristic; no external validation layer yet.
- Some modules still use `print()` instead of structured logging.
- DB path/config is still mostly default-arg based; centralized env config is incomplete.
- No explicit handling for player trades/team-context shifts mid-season.
- Legacy utility files remain (`nba_model/data/nba_data_fetcher.py`, `nba_model/data/database_utils.py`) and should be consolidated or retired.

================================================================================
RESOURCES & REFERENCES
================================================================================

**APIs Used:**
- nba_api: https://github.com/swar/nba_api
- The Odds API: https://the-odds-api.com/ (baseline ingestion implemented)

**Relevant Documentation:**
- SQLite: https://docs.python.org/3/library/sqlite3.html
- pandas: https://pandas.pydata.org/docs/
- scipy.stats: https://docs.scipy.org/doc/scipy/reference/stats.html

**Academic References:**
- "Forecasting Sports Outcomes" papers
- Market efficiency in sports betting
- Time-series analysis techniques

**Industry Standards:**
- -110 odds = 52.4% breakeven rate
- Kelly Criterion for bet sizing
- Sharpe ratio for risk-adjusted returns

================================================================================
END OF NOTES
================================================================================
